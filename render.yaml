services:
  - type: web
    name: adina-bot-backend
    plan: free
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    envVars:
      - key: API_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: adina-db
          property: connectionString
      - key: DEMO_MODE
        value: "false"
      - key: PORT
        value: "8000"
      - key: HUNTER_API_KEY
        sync: false
      - key: GOOGLE_CSE_API_KEY
        sync: false
      - key: GOOGLE_CSE_CX
        sync: false
    healthCheckPath: /health

databases:
  - name: adina-db
    plan: free
    databaseName: adina
