# ─── Required ────────────────────────────────────────────────────────────────

# Secret key for authenticating /api/* requests (sent as x-api-key header)
API_KEY=change-me-to-a-long-random-string

# Postgres connection string (provided by Render, Railway, etc.)
DATABASE_URL=postgresql://user:password@host:5432/adina

# ─── Gmail / OAuth ───────────────────────────────────────────────────────────

# Directory where gmail_credentials.json and gmail_token.json are stored
# Point this to a persistent volume in production
CREDENTIALS_DIR=/data/credentials

# Must match the redirect URI registered in Google Cloud Console
# Update this to your production domain
OAUTH_REDIRECT_URI=https://your-backend.onrender.com/oauth/callback

# Max emails the system will send per day
GMAIL_DAILY_LIMIT=100

# ─── Frontend / CORS ─────────────────────────────────────────────────────────

# The production URL of your frontend (added to CORS allowed origins)
FRONTEND_URL=https://your-frontend.onrender.com

# ─── Feature Flags ───────────────────────────────────────────────────────────

# Set to true to block all real Gmail sends (dry-run still works)
DEMO_MODE=false
