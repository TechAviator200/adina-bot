# ─── API Connection ──────────────────────────────────────────────────────────

# Production backend URL (no trailing slash)
VITE_API_BASE_URL=https://your-backend.onrender.com

# API key — must match the backend's API_KEY value
VITE_API_KEY=change-me-to-match-backend-api-key

# ─── Feature Flags ───────────────────────────────────────────────────────────

# Set to true to show Demo Mode badge and lock UI to dry-run only
VITE_DEMO_MODE=false
